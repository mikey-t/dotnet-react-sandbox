version: "3.8"  

services:
  postgresql:
    image: postgres:13.8
    volumes:
      - ./pg/data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: "${DB_ROOT_USER}"
      POSTGRES_PASSWORD: "${DB_ROOT_PASSWORD}"
    ports:
      - "${DB_PORT}:5432"
